package Graph

import (
	"fmt"
)

const ()

type MATRIX [][]int

func main() {

}

func setEdge(board MATRIX, p1, p2, value int) MATRIX {
	return board
}

func dijkstra(board MATRIX, start, end, size int) []int {
	var result []int
	return result
}
